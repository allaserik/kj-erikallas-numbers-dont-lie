/**
 * type DashboardState =
    | { kind: "signedOut" }
    | { kind: "setupRequired" }
    | { kind: "loading"; flags: ... }
    | { kind: "error"; message: string }
    | { kind: "ready"; data: ... };*/
